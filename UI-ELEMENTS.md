# 书签树扩展 - 界面元素命名文档

本文档详细列出界面中所有可调整的元素名称和位置，方便精准描述界面调整需求。

---

## 📐 整体布局结构

```
┌─────────────────────────────────────────────────────────────────┐
│                        背景层 (Background Layer)                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    主容器 (Container)                       │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │              搜索区域 (Search Container)              │  │  │
│  │  │  ┌─────────────────────────────────────────────┐    │  │  │
│  │  │  │            时间显示 (Ambient Time)            │    │  │  │
│  │  │  └─────────────────────────────────────────────┘    │  │  │
│  │  │  ┌─────────────────────────────────────────────┐    │  │  │
│  │  │  │             搜索框 (Search Wrapper)           │    │  │  │
│  │  │  │  [引擎标签] [输入框............] [AI按钮]     │    │  │  │
│  │  │  └─────────────────────────────────────────────┘    │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │            常用书签区 (Frequent Bookmarks)            │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │            书签树区域 (Bookmarks Tree)               │  │  │
│  │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐              │  │  │
│  │  │  │ 书签卡片 │  │ 书签卡片 │  │ 书签卡片 │   ...        │  │  │
│  │  │  └─────────┘  └─────────┘  └─────────┘              │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │                                           [设置按钮] ⚙️   │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 1️⃣ 背景层 (Background Layer)

| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **背景层** | `#background-layer` | 整个页面的背景，支持渐变色或自定义图片 |

**可调整属性：**
- 背景颜色 (`background-color`)
- 背景图片 (`background-image`)
- 模糊效果 (`filter: blur()`)

---

## 2️⃣ 主容器 (Container)

| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **主容器** | `.container` | 包含所有内容的主要容器，带毛玻璃效果 |

**可调整属性：**
- 宽度 (`width`: 96%)
- 高度 (`height`: 92vh)
- 圆角 (`border-radius`: 24px)
- 内边距 (`padding`: 40px 20px)
- 背景模糊 (`backdrop-filter: blur()`)
- 边框 (`border`)
- 阴影 (`box-shadow`)

---

## 3️⃣ 搜索区域 (Search Container)

### 3.1 搜索容器
| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **搜索容器** | `#search-container` | 搜索区域的外层容器 |
| **搜索遮罩层** | `#search-overlay` | 搜索时的背景模糊遮罩 |

### 3.2 时间显示
| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **氛围时间** | `.ambient-time` / `#ambient-time-container` | 搜索框上方的时间显示 |

**可调整属性：**
- 字体大小 (`font-size`: 13px)
- 字体粗细 (`font-weight`: 300)
- 颜色 (`color`: var(--text-secondary))
- 透明度 (`opacity`)

### 3.3 搜索框
| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **搜索框外壳** | `.search-wrapper` | 搜索框的整体容器（圆角矩形） |
| **搜索引擎选择器容器** | `#engine-picker-container` | 搜索引擎标签的容器 |
| **搜索引擎标签** | `#search-engine-label` | 显示当前搜索引擎名称（如 Google） |
| **搜索引擎下拉菜单** | `#engine-wheel` | 搜索引擎选择弹出菜单 |
| **搜索引擎选项** | `.wheel-option` | 每个搜索引擎选项 |
| **搜索输入框** | `#search-input` | 实际的文本输入框 |
| **AI 按钮** | `#ai-sidebar-btn` | 搜索框右侧的 AI 星星按钮 |
| **搜索建议下拉** | `#search-suggestions` | 搜索时的建议下拉列表 |

**搜索框可调整属性：**
- 宽度 (`width`: 58.5%)
- 最大宽度 (`max-width`: 800px)
- 圆角 (`border-radius`: 20px)
- 阴影 (`box-shadow`)
- 边框 (`border`)

### 3.4 搜索建议
| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **建议项** | `.suggestion-item` | 每条搜索建议 |
| **建议图标** | `.suggestion-icon` | 建议项的图标 |
| **建议内容** | `.suggestion-content` | 建议项的文字内容区 |
| **建议标题** | `.suggestion-title` | 书签/搜索建议的标题 |
| **建议 URL** | `.suggestion-url` | 书签的网址 |
| **建议文件夹标签** | `.suggestion-folder` | 书签所属文件夹的标签 |

---

## 4️⃣ 常用书签区 (Frequent Bookmarks)

| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **常用书签区** | `#frequent-bookmarks` | 常用书签的整体区域 |
| **常用书签项容器** | `.suggested-items` | 常用书签项的容器 |

---

## 5️⃣ 书签树区域 (Bookmarks Tree)

### 5.1 主容器
| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **书签树容器** | `#bookmarks-tree` | 书签卡片的滚动区域 |
| **顶层容器** | `.top-level-container` | 书签卡片的网格布局容器 |

### 5.2 书签卡片
| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **书签卡片** | `.bookmark-card` | 每个文件夹的卡片容器 |
| **卡片头部** | `.card-header` | 卡片顶部区域（包含图标和标题） |
| **卡片标题** | `.card-title` | 文件夹名称标题 |
| **展开按钮** | `.expand-btn` | 卡片右上角的绿色展开按钮（Mac 风格） |
| **卡片内容** | `.card-content` | 卡片内的书签列表区域 |

**书签卡片可调整属性：**
- 宽度 (`width`: 360px)
- 高度 (`height`: 380px)
- 圆角 (`border-radius`: 24px)
- 内边距 (`padding`: 24px)
- 阴影 (`box-shadow`)
- 悬停效果

### 5.3 书签项
| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **书签项** | `.bookmark-item` | 每个书签链接 |
| **书签图标** | 书签项内的 `img` 或图标 | 网站 favicon |
| **书签标题** | 书签项内的文字 | 书签名称 |

---

## 6️⃣ 设置按钮 (Settings Button)

| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **设置按钮** | `#settings-btn` | 右下角的齿轮设置按钮 |

**可调整属性：**
- 位置 (`bottom`: 24px, `right`: 24px)
- 大小 (`width/height`: 40px)
- 圆角 (`border-radius`: 12px)
- 背景 (`background`)
- 阴影 (`box-shadow`)

---

## 7️⃣ AI 侧边栏 (AI Sidebar)

| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **AI 侧边栏** | `#ai-sidebar` / `.ai-sidebar` | AI 功能的整体侧边栏 |
| **侧边栏遮罩** | `#ai-sidebar-overlay` | 侧边栏打开时的背景遮罩 |
| **侧边栏头部** | `.ai-sidebar-header` | 侧边栏顶部区域 |
| **AI 标签栏** | `.ai-tabs` | AI 服务切换标签容器 |
| **AI 标签** | `.ai-tab` | 每个 AI 服务的标签 |
| **AI 标签图标** | `.ai-tab-icon` | 标签的图标 |
| **AI 标签名称** | `.ai-tab-name` | 标签的文字名称 |
| **侧边栏操作区** | `.ai-sidebar-actions` | 新窗口、关闭按钮区域 |
| **新窗口按钮** | `#ai-open-new-window` | 在新窗口打开的按钮 |
| **关闭按钮** | `#ai-sidebar-close` | 关闭侧边栏的按钮 |
| **侧边栏内容区** | `.ai-sidebar-content` | AI iframe 显示区域 |
| **AI iframe** | `.ai-iframe` | 嵌入的 AI 服务页面 |

---

## 8️⃣ 设置弹窗 (Settings Modal)

| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **设置弹窗** | `#settings-modal` | 设置弹窗的整体容器 |
| **弹窗内容** | `.modal-content` | 弹窗的内容区域 |
| **关闭按钮** | `#close-modal` / `.close` | 弹窗关闭按钮 |
| **弹窗标题** | `.modal-content h2` | "设置" 标题 |
| **设置项** | `.setting-item` | 每个设置选项的容器 |
| **设置项标签** | `.setting-item label` | 设置项的标题标签 |
| **单选按钮组** | `.radio-group` | 单选按钮的容器 |
| **单选选项** | `.radio-option` | 每个单选项 |
| **文件上传区** | `.file-upload-container` | 背景图片上传区域 |
| **模糊滑块** | `#bg-blur` | 背景模糊程度滑块 |
| **模糊值显示** | `#blur-value` | 显示模糊数值 |

---

## 9️⃣ 文件夹展开弹窗 (Folder Modal)

| 元素名称 | CSS ID/Class | 描述 |
|---------|-------------|------|
| **文件夹弹窗** | `#folder-modal` | 文件夹展开弹窗的容器 |
| **弹窗内容** | `.folder-modal-content` | 弹窗内容区域 |
| **弹窗头部** | `.folder-modal-header` | 弹窗顶部区域 |
| **文件夹图标** | `.folder-modal-icon` | 文件夹图标 |
| **文件夹标题** | `.folder-modal-title` | 文件夹名称 |
| **关闭按钮** | `#close-folder-modal` | 弹窗关闭按钮 |
| **弹窗主体** | `.folder-modal-body` | 书签列表区域 |

---

## 🎨 主题相关变量

### 浅色主题 (Light Theme)
```css
--primary-color: #ae5630;      /* 主题色（陶土橙） */
--text-color: #1a1a18;         /* 主文字颜色 */
--text-secondary: #6b6a68;     /* 次要文字颜色 */
--bg-overlay: rgba(245, 245, 240, 0.4);  /* 背景叠加色 */
--card-bg: #FFFFFF;            /* 卡片背景 */
--card-hover: #F5F5F0;         /* 卡片悬停背景 */
--border-color: rgba(0, 0, 0, 0.08);     /* 边框颜色 */
--modal-bg: #FFFFFF;           /* 弹窗背景 */
--modal-text: #1a1a18;         /* 弹窗文字 */
```

### 深色主题 (Dark Theme)
```css
--primary-color: #D97757;      /* 主题色 */
--text-color: #F2F0E9;         /* 主文字颜色 */
--text-secondary: #A8A6A3;     /* 次要文字颜色 */
--bg-overlay: rgba(26, 21, 35, 0.4);     /* 背景叠加色 */
--card-bg: #221D2A;            /* 卡片背景 */
--card-hover: #2D2638;         /* 卡片悬停背景 */
--border-color: rgba(217, 119, 87, 0.12); /* 边框颜色 */
--modal-bg: #221D2A;           /* 弹窗背景 */
--modal-text: #F2F0E9;         /* 弹窗文字 */
```

---

## 📝 调整示例用语

以下是一些精准描述的示例：

| 需求描述 | 对应元素 |
|---------|---------|
| "搜索框太窄了" | **搜索框外壳** (`.search-wrapper`) 的 `width` |
| "卡片之间间距太大" | **顶层容器** (`.top-level-container`) 的 `gap` |
| "设置按钮位置太靠边" | **设置按钮** (`#settings-btn`) 的 `bottom/right` |
| "AI标签栏高度太高" | **AI标签栏** (`.ai-tabs`) 或 **侧边栏头部** (`.ai-sidebar-header`) |
| "弹窗文字看不清" | **弹窗内容** (`.modal-content`) 的 `color` |
| "卡片标题颜色太淡" | **卡片标题** (`.card-title`) 的 `color` |
| "搜索框圆角想更大" | **搜索框外壳** (`.search-wrapper`) 的 `border-radius` |
| "时间显示太透明" | **氛围时间** (`.ambient-time`) 的 `opacity` |

---

## 📁 文件位置

- **HTML 结构**: `newtab.html`
- **样式文件**: `styles.css`
- **脚本文件**: `script.js`
